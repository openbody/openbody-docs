---
title: OpenBody™
description: Own your health & fitness data — and build apps on an open, portable format backed by a real standard.
template: splash
hero:
  tagline: Your training history belongs to you. One open, portable format that any app can read and write — losslessly, without platform lock-in. Convert your data out of any silo, or build your app on it natively.
  image:
    file: ../../assets/openbody-mark.svg
  actions:
    - text: Convert your data
      link: /tools/convert/
      icon: rocket
      variant: primary
    - text: Build on OpenBody
      link: /build/
      icon: seti:json
      variant: secondary
    - text: OpenBody in five minutes
      link: /five-minutes/
      icon: open-book
      variant: minimal
    - text: View on GitHub
      link: https://github.com/openbody/openbody
      icon: external
      variant: minimal
---

import { Card, CardGrid, Aside, LinkCard } from '@astrojs/starlight/components';
import meta from '../../generated/spec-meta.json';

<p>
  <span class="ob-status">DRAFT · v{meta.specVersion} · pre-v1.0</span>
</p>

## Get your data out

Years of training live inside whichever apps you happened to log them in — the lifting
app's schema, the watch's activity files, the climbing app's ticks, each with its own
exercise names and export quirks. Switching apps means losing history; building on top of
an app means building on an API that can change under you.

**[Try it now](/tools/convert/):** upload a real Hevy CSV export and watch it become
portable OpenBody JSON — parsed, converted, and downloadable **entirely in your browser.
Nothing leaves your machine.** No signup, no upload, no account. (Hevy is the first
in-browser converter; the [open-source library](/mapping/) behind it already maps Strong,
Strava, Apple Health, and Garmin FIT exports too.)

The timing matters. Platforms keep tightening what third parties may do with *your* data —
Strava's API restrictions being the most visible recent example — while the **EU Data Act**
phases in the other direction, requiring connected products to make user data accessible
(with design obligations applying to products placed on the market from **September 2026**).
Portable, well-specified formats are how those rights become usable in practice.

## What OpenBody gives you

<CardGrid>
  <Card title="A lossless data model — both pillars" icon="seti:pipeline">
    One format for **telemetry** (heart rate, sleep, GPS, body composition) *and*
    **structured training** — and structured training means *all of it*: sets and
    supersets, runs, rides and swims, holds and intervals, climbing sends, match and race
    results, mobility and breathwork, planned vs performed. One scoring model spans a
    barbell set and a marathon; the
    [coverage corpus](https://github.com/openbody/openbody/tree/main/conformance/corpus)
    proves it on everything from Hyrox to kabaddi to freediving. Incumbent → OpenBody is
    **lossless by design**: anything without a canonical home is preserved via namespaced
    extensions or the opaque fallback. Nothing is dropped.
  </Card>
  <Card title="Canonical exercise identity" icon="approve-check">
    One app's “Bench Press (Barbell)” and another's “Barbell Bench Press” become the
    **same canonical movement**: `bench-press.barbell.flat`. A curated, CC0
    [exercise registry](/registry/exercises/) of 740+ movements with structured facets and
    crosswalks — and mappers resolve source names to canonical ids, falling back to a
    lossless opaque string when they can't.
  </Card>
  <Card title="Open & vendor-neutral" icon="github">
    **Fully open, not open-core**: spec under OWFa 1.0, reference code Apache-2.0, registry
    data CC0 — implementable by anyone, in any language, no strings attached. Stewarded by
    Thabit Labs with intent to transfer to a neutral foundation as adoption warrants.
    See [Governance](/governance/).
  </Card>
</CardGrid>

## Start here — for your data

<CardGrid>
  <LinkCard title="Convert your workout data" href="/tools/convert/" description="Hevy, Strong, Apple Health, or Strava exports → portable OpenBody JSON, entirely in your browser. The fastest way to see what OpenBody is." />
  <LinkCard title="OpenBody in five minutes" href="/five-minutes/" description="The whole core in seven schema-valid JSON examples — a measurement, a set, a run, a session, a superset, planned vs performed, a climbing send." />
</CardGrid>

## Start here — for app builders

<CardGrid>
  <LinkCard title="Build on OpenBody" href="/build/" description="Adopt OpenBody as your native data model: a proven schema, a CC0 exercise database of 800+ movements, and import from five sources on day one." />
  <LinkCard title="Getting started" href="/getting-started/" description="Validate and normalize records with the TypeScript reference implementation, then run the conformance vectors." />
  <LinkCard title="Mapping guides" href="/mapping/" description="Real incumbent → OpenBody mappings: Hevy, Strong, Strava, Apple Health, FIT." />
  <LinkCard title="The exercise registry" href="/registry/exercises/" description="Canonical movement ids, facets, progression relationships, and per-app name crosswalks — CC0." />
</CardGrid>

## Built on a real standard

Tools are only as good as the format underneath them. OpenBody is a **canonical data model
plus mapping semantics** — defined independently of any serialization, with **JSON (and a
published JSON Schema) as the primary, first-class binding**, published conformance
profiles, and executable test vectors. Every claim above is specified, not improvised.

<CardGrid>
  <LinkCard title="Read the standard" href="/specification/" description={`The rendered, canonical SPEC.md (v${meta.specVersion}) plus the JSON Schema and changelog.`} />
  <LinkCard title="Concepts" href="/concepts/data-model/" description="The data model, the two pillars, exercise identity, and canonicalization — explained plainly." />
  <LinkCard title="Conformance" href="/conformance/" description="Profiles, the test vectors, and how to claim “OpenBody Compatible”." />
  <LinkCard title="Registry" href="/registry/" description="The CC0 registries: curated exercise ids/facets and measurement-type tokens." />
</CardGrid>

<Aside type="caution" title="Draft standard — in private review">
OpenBody is a **pre-v1.0 draft (spec v{meta.specVersion})**, currently in private review and being
dogfooded before a public draft release. **Breaking changes are still possible before
v1.0.** Nothing here should be read as a claim of broad adoption or a stable, frozen
standard. Status and history live in the [changelog](/specification/changelog/).
</Aside>

---

<p style="font-size: var(--sl-text-sm); opacity: 0.8;">
“OpenBody” is a vendor-neutral standard stewarded by Thabit Labs. The steward is not the
brand on the wire. Specification: <strong>OWFa 1.0</strong> · Reference code:
<strong>Apache-2.0</strong> · Registry data: <strong>CC0</strong>. See
<a href="/licensing/">Licensing</a>.
</p>
